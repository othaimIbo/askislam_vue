module.exports = {
    "presets": [
        ["@babel/preset-env"],

        [
            "babel-preset-vite",
            {
                "env": true,
                "glob": false
            }
        ]
    ]
}